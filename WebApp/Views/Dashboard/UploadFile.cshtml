@model WebApp.Models.FileUploadModel
@using WebApp.Controllers

<div class="row">
    <div class="col-md-6 col-md-push-2">
        <div class="form-horizontal">
            @*@using (Html.BeginForm("UploadFile", "Dashboard", FormMethod.Post, new { id = DashboardController.UploadFileForm, encType = "multipart/form-data" }))
            {*@
            <div class="form-group">
                <span>Browse Photo:</span>
                @Html.TextBoxFor(m => m.FileAttach, new { type = "file", accept = "image/x-png,image/jpg,image/jpeg", placeholder = Html.DisplayNameFor(m => m.FileAttach), @id = "formFile", @class = "form-control" })
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Upload" onclick="uploadFile()" class="btn btn-danger" />
                </div>
            </div>
            @*}*@
        </div>
    </div>
</div>